{
  "name": "@flixy/web",
  "version": "0.1.0",
  "private": true,
  "description": "Static landing page for Flixy at flixy.app",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "pnpm --filter @flixy/mobile web",
    "preview": "sirv dist --single --port 3000",
    "typecheck": "echo \"@flixy/web: no typecheck needed\" && exit 0",
    "test": "echo \"@flixy/web: no tests\" && exit 0"
  },
  "devDependencies": {
    "sirv-cli": "^3.0.0"
  }
}
